import {InsertShadow} from './insert-shadow';

describe('InsertShadow', () => {
    it('should create an instance', () => {
        const directive = new InsertShadow();
        expect(directive).toBeTruthy();
    });
});
